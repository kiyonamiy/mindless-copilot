package {{rootPackageName}}.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

@Data
@Builder
@AllArgsConstructor
@NoArgsConstructor
@Schema(description = "{{tableComment}}-创建 VO")
public class {{typeName}}CreateReqVO {
    {{#each columns}}
    @Schema(description = "{{this.comment}}")
    private {{this.type}} {{this.name}};
    {{/each}}
}