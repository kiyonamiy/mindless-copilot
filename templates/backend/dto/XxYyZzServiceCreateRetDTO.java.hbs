package {{rootPackageName}}.dto;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Builder;
import lombok.Data;

@Data
@Builder
public class {{pascalTableName}}ServiceCreateRetDTO {
    {{#each columns}}
    /**
     * {{this.comment}}
     */
    private {{this.type}} {{this.name}};
    {{/each}}
}